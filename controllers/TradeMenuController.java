package controllers;

import models.App;
import models.Game;

public class TradeMenuController {
    App app = App.getInstance();
    Game game = App.getInstance().getCurrentGame();

    // username, itemName, amount, price
    // username, itemName, amount, itemName, amount
    public Result tradeRequest(String... inputs) {
        Player
    }
}
